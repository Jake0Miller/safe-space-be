addons:
  postgresql: '9.6'
before_script:
  - cp config/database.yml.travis config/database.yml
dist: trusty
language: ruby
rvm:
  - 2.4.1
script:
  - bundle exec rails db:migrate RAILS_ENV=test
  - bundle exec rspec
services:
  - postgresql
  - redis-server
